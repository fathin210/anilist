import { Theme } from "@emotion/react";

const theme: Theme = {
  colors: {
    primary: "#1fa58d",
    accent: "#fea009",
    text: "#2b2d42",
    white: "#fff",
    black: "#000",
  },
};

export default theme;
